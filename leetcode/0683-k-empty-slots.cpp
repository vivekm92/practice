#include <bits/stdc++.h>

using namespace std;

class Solution {
private:
	// Approach 1
    int solveKEmptySlots(vector<int> &bulbs, int k) {
        
    }
public:
    int kEmptySlots(vector<int>& bulbs, int k) {
        return solveKEmptySlots(bulbs, k);
    }
};

// Driver Code for testing
int main() {

    return 0;
}
